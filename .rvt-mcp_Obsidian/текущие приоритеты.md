# Текущие приоритеты

- Проверять живой Revit-сценарий после полного перезапуска Revit и pyRevit.
- Не создавать `DB.Transaction` при `doc is None`; возвращать `503`.
- Для последовательного открытия моделей проверять состояние моста после каждой операции.
